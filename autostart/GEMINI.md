# Autostart

This directory contains configuration files and scripts used to automatically launch the AutomateLinux daemon and related services upon system login. This ensures the automation features are active as soon as the user environment is ready.
