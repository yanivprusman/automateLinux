# Sudoers

This directory contains custom `sudoers` rules required for project components to interact with restricted system resources (like `/dev/input/` or system service management) without requiring manual password entry.
